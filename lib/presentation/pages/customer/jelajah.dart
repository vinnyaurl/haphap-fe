import 'package:flutter/material.dart';

class JelajahPage extends StatelessWidget {
  const JelajahPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Jelajah coming soon'),
      ),
    );
  }
}